{
  "name": "kntn-dev-mcp",
  "version": "0.1.0",
  "private": true,
  "description": "Kintone Customize Development Support MCP Server",
  "license": "UNLICENSED",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rimraf dist",
    "start": "node dist/index.js",
    "lint": "biome check .",
    "format": "biome format --write .",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.17.5"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.2.3",
    "@types/node": "^22.7.4",
    "typescript": "^5.6.2",
    "rimraf": "^6.0.1",
    "vitest": "^2.1.3"
  }
}
